sudo: true
language: cpp
os: linux
dist: xenial
compiler:
  - g++
before_install:
  - sh scripts/install-protobuf.sh
before_script:
  - mkdir build
  - cd build
  - cmake ..
script:
  - make
branches:
  only:
    - master